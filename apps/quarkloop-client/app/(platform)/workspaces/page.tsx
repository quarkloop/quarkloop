import { WorkspaceList } from "@quarkloop/components";

const WorkspaceListPage = async () => {
    return (
        <div className="flex-1 flex flex-col">
            <WorkspaceList />
        </div>
    );
};

export default WorkspaceListPage;
