import { UserLinkedAccountList } from "@quarkloop/components";

export const dynamic = "force-dynamic";

const UserLinkedAccountsPage = (props: any) => <UserLinkedAccountList />;

export default UserLinkedAccountsPage;
